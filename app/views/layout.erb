<!DOCTYPE html>
<html>

  <head>
    <title>Bookmark Manager2</title>
  </head>

  <body>

    <% if flash[:notice] %>
      <div id="notice">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <% if flash[:errors] && !flash[:errors].empty? %>
      Sorry, there were the following problems with the form:
      <ul id="errors">
        <% flash[:errors].each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    <% end %>

    <%= yield %>

  </body>

</html>